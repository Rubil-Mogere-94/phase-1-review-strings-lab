// Write your code in this file!
// Define currentUser with your name or any string
let currentUser = 'Alice';

// welcomeMessage should contain "Welcome to Flatbook, " and the value of currentUser, ending with an exclamation point
let welcomeMessage = `Welcome to Flatbook, ${currentUser}!`;

// excitedWelcomeMessage should contain "WELCOME TO FLATBOOK, " and the value of currentUser, in uppercase, ending with an exclamation point
let excitedWelcomeMessage = `WELCOME TO FLATBOOK, ${currentUser.toUpperCase()}!`;

// shortGreeting should contain "Welcome, " and the first initial of the currentUser, ending with an exclamation mark
let shortGreeting = `Welcome, ${currentUser[0]}!`;
